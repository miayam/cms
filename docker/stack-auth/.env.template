# Stack Auth Self-Hosted Environment Configuration

# === Database Configuration ===
# Connection string for PostgreSQL
DATABASE_URL=postgresql://stack_auth:stack_auth_password@postgres:5432/stack_auth

# === Server Configuration ===
# Public URL for Stack Auth API
NEXT_PUBLIC_STACK_API_URL=http://localhost:8102

# === Security Settings ===
# Generate this with: openssl rand -hex 32
STACK_SECRET_SERVER_KEY=replace_with_random_32_byte_hex_string

# === Email Configuration ===
# For development with Inbucket (default)
SMTP_HOST=inbucket
SMTP_PORT=2500
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_NAME=Stack Auth
SMTP_FROM_EMAIL=noreply@example.com

# For production with MailerSend (uncomment and fill in when ready)
# SMTP_HOST=smtp.mailersend.net
# SMTP_PORT=587
# SMTP_SECURE=true
# SMTP_USER=your_mailersend_username
# SMTP_PASSWORD=your_mailersend_api_key_here
# SMTP_FROM_NAME=Stack Auth
# SMTP_FROM_EMAIL=noreply@your-domain.com

# === Environment Selection ===
# Set to 'dev' or 'prod' to determine which services to run
# This controls whether to use Inbucket (dev) or MailerSend (prod)
STACK_ENV=dev

# === Advanced Settings (Optional) ===
# Port configuration (if you need to change defaults)
# DASHBOARD_PORT=8101
# API_PORT=8102

# Session configuration
# SESSION_SECRET=generate_another_random_string_for_sessions

# CORS settings (if API is hosted on different domain)
# CORS_ORIGIN=https://your-frontend-domain.com
